# Amazon ASIN 标签阈值配置（示例）
# 复制为 config/labeling.yaml 后可覆盖默认值。

thresholds:
  r: 1.6          # 销售比阈值（>= 1.6 表示销售翻 1.6 倍）
  p: 0.2          # BSR 百分位下降 20% 触发（0-1 表示百分比）
  delta: 1.0      # BSR 排名需提升至少 1 名

windows:
  observation_days: 28
  forecast_days: 30

notes:
  - "修改后请同步更新训练与推理环境变量"
  - "验收阶段建议将实际指标记录在 docs/release_checklist.md"
